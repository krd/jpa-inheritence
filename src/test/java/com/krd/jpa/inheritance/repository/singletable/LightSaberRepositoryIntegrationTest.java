package com.krd.jpa.inheritance.repository.singletable;

import org.springframework.boot.test.context.SpringBootTest;

import static org.junit.Assert.assertTrue;

@SpringBootTest
public class LightSaberRepositoryIntegrationTest {

//  @Autowired
//  LightSaberRepository lightSaberRepository;
//
//  @Test
//  public void whenLightSaber_thenIsPersisted() {
//    LightSaber lightSaber = new LightSaber();
//    lightSaber.setColor("RED");
//    lightSaber.setDamage(200);
//    lightSaber.setStyle("DOUBLE-BLADED");
//    lightSaberRepository.save(lightSaber);
//
//    Set<LightSaber> found = lightSaberRepository.findByColorAndStyle("RED", "DOUBLE-BLADED");
//    assertTrue(found.size() > 0 );
//  }
}
